<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>VARIANTS DETAILS</title>
</head>
<body class="bg-black">
    <div class="py-8 text-center"> 
        <h1 class="mb-4 text-3xl font-extrabold text-white md:text-5xl lg:text-6xl inline-block"> 
            <span class="text-transparent bg-clip-text bg-gradient-to-r to-emerald-600 from-sky-400">VARIANTS</span>
        </h1>
    </div>

    <div class="relative overflow-x-auto shadow-md sm:lg">
        <table class="w-full text-sm md:text-lg text-left rtl:text-right text-gray-500 text-white"> 
            <thead class="text-xs md:text-sm lg:text-base text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
                <tr>
                    <th scope="col" class="px-6 py-3">
                        Size
                    </th>
                    <th scope="col" class="px-6 py-3">
                        Color
                    </th>
                    <th scope="col" class="px-6 py-3">
                        Material
                    </th>
                </tr>
            </thead>
            <tbody>
                <% variants.forEach(function(variant) { %>
                    <tr class="odd:bg-white odd:dark:bg-gray-900 even:bg-gray-50 even:dark:bg-gray-800 border-b dark:border-gray-700">
                        <td class="px-6 py-4 font-medium whitespace-nowrap">
                            <%= variant.size %>
                        </td>
                        <td class="px-6 py-4">
                            <%= variant.color %>
                        </td>
                        <td class="px-6 py-4">
                            <%= variant.material %>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
</body>
</html>
